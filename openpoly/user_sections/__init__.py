"""User-supplied section impls. See README.md for trust boundary disclaimer.

The registry scans this package alongside openpoly.sections. Files here can
import from openpoly.sections._base and define classes satisfying the Section
Protocol. Restart the backend after adding files.
"""
